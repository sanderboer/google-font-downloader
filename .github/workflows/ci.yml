name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "CI workflow placeholder - linting and testing occur locally"
